---
tags:
  - "#ai-allow"
  - test
---

# Tagged Allow Test

This root-level file has the `#ai-allow` global tag.

It should be **accessible** even when default policy is deny, because the global allow tag overrides the default policy.
