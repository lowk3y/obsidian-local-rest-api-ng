---
tags: [draft, documentation]
---

# Draft README

This is a draft file at the root level. Should be **DENIED** by name rule `deny name draft-*`.

The `draft-*` glob pattern matches any file whose basename starts with "draft-".

If you can read this through the API, the document name filter is not working.
